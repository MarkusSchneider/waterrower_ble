# WaterRower Training System - Configuration

# Server Configuration
PORT=3000

# Garmin Connect Credentials (optional - can also be configured via web UI)
# GARMIN_EMAIL=your@email.com
# GARMIN_PASSWORD=yourpassword

# WaterRower Serial Port (auto-detected if not specified)
# WATERROWER_PORT=/dev/ttyACM0

# Data Storage
FIT_FILES_DIR=./data/fit-files

# Heart Rate Monitor (optional)
# HR_MONITOR_NAME=Polar H10

# Debug logging (set to enable specific modules)
# DEBUG=MAIN,WEB_SERVER,TRAINING_SESSION,HR_MONITOR,FIT_GENERATOR,GARMIN_UPLOADER
